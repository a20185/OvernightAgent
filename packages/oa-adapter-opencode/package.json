{
  "name": "oa-adapter-opencode",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "pretest": "tsc --build . --force",
    "test": "vitest run",
    "lint": "eslint src test",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "oa-core": "workspace:*"
  }
}
