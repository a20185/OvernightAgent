{
  "name": "oa-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "oa": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "dev": "tsx src/cli.ts",
    "pretest": "tsc --build . --force",
    "test": "vitest run",
    "lint": "eslint src test",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "commander": "~14.0.3",
    "oa-core": "workspace:*"
  }
}
