# /oa-status — Codex binding

```sh
oa status --json $ARGUMENTS
```

Render the JSON for the user: plan id, source (socket vs events), current
task/step, elapsed, last event kind if snapshot-derived.
