# /oa-status — opencode binding

```sh
oa status --json $ARGUMENTS
```

Render the JSON for the user. Snapshot-derived status is also acceptable if
the daemon is not running — the CLI falls back automatically.
